#!/usr/bin/python3
def add_tuple(tuple_a=(), tuple_b=()):
    x1 = tuple1[0] if len(tuple1) > 0 else 0
    x2 = tuple1[1] if len(tuple1) > 1 else 0
    y1 = tuple2[0] if len(tuple2) > 0 else 0
    y1 = tuple2[1] if len(tuple2) > 1 else 0
    return (x1 + y1, x2 + y2)
